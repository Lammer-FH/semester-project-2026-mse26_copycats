to be added


## MySQL
Start DB:
```
docker compose up -d
```

Connect to DB:
```
docker exec -it hotelcopycat-mysql mysql -u hoteluser -p hotelcopycat

## Ionic Vue Frontend
- cd MSE26-HotelCopyCat-Frontend
- "npm install" to install all dependencies
- Install Ionic CLI: npm install -g @ionic/cli
- "ionic serve" to start