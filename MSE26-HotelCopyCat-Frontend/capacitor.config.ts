import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MSE26-HotelCopyCat-Frontend',
  webDir: 'dist'
};

export default config;
